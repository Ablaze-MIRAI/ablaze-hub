<%= render 'flow_base' %>

<div class="flex flex-col justify-start h-screen">
  <h2 class="text-3xl mb-6">Login</h2>
  <% if @flow %>
    <%= render Flows::FlowComponent.new(flow: @flow, submit_path: @submit_path) %>
  <% end %>
</div>